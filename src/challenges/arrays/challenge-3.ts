/* *-*-*-*-*-*-*-*-*-*-* Challenge 3 ------------------
Create an array of the two boolean values
*/

let istina: boolean[] = [true && true && true && 1 && false && 0 && NaN, true];
console.log(istina); // false
