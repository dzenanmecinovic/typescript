/* *-*-*-*-*-*-*-*-*-*-* Challenge 4 ------------------
Create a variable with the type Boolean and assign it to the logical OR operator comparing two values
*/

let isRainy: boolean = true || false;
// true || true;    correct
// false || false;  correct
