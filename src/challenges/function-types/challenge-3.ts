/* *-*-*-*-*-*-*-*-*-*-* Challenge 3 ------------------
Write a function that can accept number, string and boolean as argument types and returns a string type
*/

function treci(arg: number | string | boolean): string {
  return `${arg}`;
  // ili arg.toString();
}
