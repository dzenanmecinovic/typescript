/* *-*-*-*-*-*-*-*-*-*-* Challenge 3 ------------------
Create an interface called ProductName and a second interface called ProductPrice
that extends the first interface. Finally create an object from the second interface
*/

// interface ProductName {
//   name: string;
// }

// interface ProductPrice extends ProductName {
//   price: number;
// }

// const proizvod: ProductPrice = { name: "Monitor", price: 190 };
