/* *-*-*-*-*-*-*-*-*-*-* Challenge 3 ------------------
Create a function that returns the length of a string array or undefined if the argument is undefined
*/

// function arrLength(arr: string[] | undefined): number | undefined {
//   return arr && arr?.length;
// }
