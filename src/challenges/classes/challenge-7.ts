// /* *-*-*-*-*-*-*-*-*-*-* Challenge 7 ------------------
// Create a product class with 2 properties, name and number of students.
// Add a method to this class that would return the string “Best Seller”
// if a course has equal to or more than 3000 students, otherwise, it should print “Not so much”
// */

// class Product {
//   name: string;
//   numOfStudents: number;
//   constructor(name: string, numOfStudents: number) {
//     this.name = name;
//     this.numOfStudents = numOfStudents;
//   }

//   seller(): string {
//     return this.numOfStudents >= 3000 ? "Best Seller" : "Not so much";
//   }
// }

// const courseProduct = new Product("JavaScript Course", 3000);
// console.log(courseProduct.seller());
