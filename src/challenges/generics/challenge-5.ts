/* *-*-*-*-*-*-*-*-*-*-* Challenge 5 ------------------
Create a function whose return type is a generic array of array of numbers. Add some values as well.
*/

function genericArr5(): Array<Array<number>> {
  let numArr = [];
  numArr.push([15]);
  numArr.push([35]);
  return numArr;
}
