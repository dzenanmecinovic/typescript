/* *-*-*-*-*-*-*-*-*-*-* Challenge 3 ------------------
Create a function that accepts a single employee object with only one property of name (created with an object type)
or an array of employee objects created with the same object type. If an object is passed,
it returns the value of the name property,if an array is passed, it returns the array length
*/

// type EmployeeAccount = {
//   name: string;
// };

// const radnik1: EmployeeAccount = {
//   name: "Zaposleni",
// };

// function employeeFunc(
//   arg: EmployeeAccount | EmployeeAccount[]
// ): number | string {
//   if (Array.isArray(arg)) {
//     return arg.length;
//   } else {
//     return arg.name;
//   }
// }

// console.log(employeeFunc([radnik1, radnik1]));
