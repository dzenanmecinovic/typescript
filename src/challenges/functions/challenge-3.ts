/* *-*-*-*-*-*-*-*-*-*-* Challenge 3 ------------------
Create an arrow function that returns the addition of a number and a boolean type
*/

// let addition = (num: number, bool: boolean): number => {
//   return num + bool;
// };
