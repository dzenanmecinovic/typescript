/* *-*-*-*-*-*-*-*-*-*-* Challenge 6 ------------------
Create a function that takes an array of numbers, then returns it.
Does not change the array. If a string is passed, it throws a type error
*/

function arr(arr: number[]): number[] {
  return arr;
}

console.log(arr([1, 2, 3]));
// console.log(arr([1, 2, 3, "s"]));
