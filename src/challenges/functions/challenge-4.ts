/* *-*-*-*-*-*-*-*-*-*-* Challenge 4 ------------------
Create a function that makes words plural by adding "s"
*/

function makePlural(word: string): string {
  return word + "s";
}

console.log(makePlural("kid")); // kids
