/* *-*-*-*-*-*-*-*-*-*-* Challenge 4 ------------------
What will the following variable assignment result in?
*/

// let bool: true = false; // type 'false' is not assignable to type 'true'.

// Boolean = true | false;
// true != false;
// false != true;

/* when we talk about Boolean, true and false are values.
but in case of talking about TRUE and FALSE as a types, they are not values. */
