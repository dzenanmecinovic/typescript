/* Object-oriented programming revolves objects.
Each object can contain data which is also called state. */

// constructor - the name is actually self-explanatory. it is used to construct a instance of a class.

// class Robot {
//   id: number;
//   model: string;
//   name: string;

//   constructor(id: number, model: string, name: string) {
//     this.id = id;
//     this.model = model;
//     this.name = name;
//   }
//   walk(): void {
//     console.log(`This robot can walk.`);
//   }
// }

// let billyRobot = new Robot(555, "G28-K", "Billy");
// console.log(billyRobot);

// billyRobot.walk();

// console.log(billyRobot instanceof Robot);
