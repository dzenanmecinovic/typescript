// never type basically says that the value will never occur.

function chatFeed(): never {
  while (true) {
    // Grab the messages and display them on the screen.
  }
}

chatFeed();

console.log("Chat Disconnected");
