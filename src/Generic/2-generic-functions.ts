/*  
    the idea behind a 'generic' is that we are 
    creating a type that could work with any data
*/

// this function return the second-indexed item of an array
// function getArrayItem<T>(item: Array<T>): T {
//   return item[1];
// }

// let number = getArrayItem<number>([1, 2, 3, 4, 5]);
// let someStr = getArrayItem<string>(["Hi", "Hey", "Hello", "There"]);
// console.log(someStr);
