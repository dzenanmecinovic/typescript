// enum GitRepo {
//   NotCommitted,
//   Committed,
// }

// function isRepoCommitted(repo: GitRepo): boolean {
//   return repo === null || repo === 0;
// }

// let gitCommit = isRepoCommitted(0);
// console.log(gitCommit);
